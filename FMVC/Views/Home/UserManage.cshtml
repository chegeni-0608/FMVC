@model IEnumerable<FMVC.Models.Students>

@{
    ViewBag.Title = "مدیریت پرسنل";
}

<h2>اسامی پرسنل </h2>

<p>
    @Html.ActionLink("ثبت پرسنل جدید", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.StudentId)*@
            آیدی
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.StudentName)*@
            نام
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.StudentFamily)*@
            نام خانوادگی
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.StudentAge)*@
            سن
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StudentId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StudentName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StudentFamily)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StudentAge)
        </td>
        <td>
            @Html.ActionLink("ویرایش", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("جزئیات", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("حذف", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
